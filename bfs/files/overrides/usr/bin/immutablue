#!/bin/bash 
# Install dir for Immutablue 
INSTALL_DIR=/usr/libexec/immutablue/just
cd ${INSTALL_DIR} || exit 1

# exec just directly
if [[ "$(type just &>/dev/null)" -eq 0 ]]
then
    exec just "${@}"
else
    echo "just is somehow not present in \$PATH"
    exit 1
fi

