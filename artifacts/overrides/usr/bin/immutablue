#!/bin/bash 
# Install dir for Immutablue 
INSTALL_DIR=/usr/libexec/immutablue/just
cd ${INSTALL_DIR} || exit 1

# exec justt directly
if [[ $(type just 2>/dev/null /dev/null) -eq 0 ]]
then
    exec just "${@}"
else
    echo "just is somehow now present in \$PATH"
    exit 1
fi

