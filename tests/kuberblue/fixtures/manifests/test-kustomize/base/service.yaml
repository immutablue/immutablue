apiVersion: v1
kind: Service
metadata:
  name: kustomize-service
spec:
  selector:
    app: test-kustomize
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  type: ClusterIP