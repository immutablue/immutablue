# SOPS configuration for this cluster
#
# Replace the Age recipient with your public key from: age-keygen

creation_rules:
  - path_regex: .*\.sops\.(yaml|json)$
    encrypted_regex: ^(data|stringData|authkey|token|password|secret|key)$
    age: "age1replace-with-your-public-key"
