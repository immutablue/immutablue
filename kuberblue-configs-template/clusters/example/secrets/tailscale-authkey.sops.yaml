# EXAMPLE — This file would be encrypted with SOPS in a real deployment.
# To encrypt: sops --encrypt --in-place tailscale-authkey.sops.yaml
#
# Plaintext structure (encrypt before committing!):
# tailscale:
#   authkey: "tskey-auth-YOUR-KEY-HERE"

tailscale:
    authkey: "REPLACE_AND_ENCRYPT_WITH_SOPS"
