# EXAMPLE — This file would be encrypted with SOPS in a real deployment.
# To encrypt: sops --encrypt --in-place flux-git-auth.sops.yaml
#
# For SSH deploy key:
# git:
#   identity: |
#     -----BEGIN OPENSSH PRIVATE KEY-----
#     ...
#     -----END OPENSSH PRIVATE KEY-----
#   known_hosts: "github.com ssh-ed25519 AAAA..."
#
# For HTTPS PAT:
# git:
#   username: "git"
#   password: "ghp_xxxxxxxxxxxx"

git:
    identity: "REPLACE_AND_ENCRYPT_WITH_SOPS"
    known_hosts: "REPLACE_AND_ENCRYPT_WITH_SOPS"
